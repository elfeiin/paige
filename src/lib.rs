#![allow(dead_code, non_camel_case_types, unused_imports)]

mod props;
use props::*;

mod attrs;
use attrs::*;

mod tag;
use tag::*;

mod element;
use element::*;

mod page;
use page::*;

#[cfg(test)]
mod tests;